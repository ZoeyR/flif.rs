language: rust
rust:
  - stable
  - beta
  - nightly
script:
  - cargo test --verbose
  - cd flif-cli; cargo run --release --verbose -- decode -i ../resources/sea_snail.flif
matrix:
  allow_failures:
    - rust: nightly